# 学习目标管理台 · 上线说明

## 访问链接（跨设备 · 在线同步）
**https://workbuddy.link/p/7qhgaaNofRgQWdP4cEo78w**

在电脑/手机浏览器或 WorkBuddy App 打开即用。数据存在资料库云端，**换设备或换浏览器打开数据都在**，无需导出备份。

## 它是什么
一个专门管理「有终点、有总量」学习目标的工具——背完 2000 个单词、读完 440 页的书、上完 60 节网课——而不是无限期习惯打卡。

## 数据真正存在线上
建了 3 张资料库数据表，页面用 SDK 直接读写：
- **学习目标** `o4a6Q9B5udQj6Y0KVwZ5hg`
- **打卡记录** `VXXupgHfclBlQFYMwLYSjv`
- **周复盘** `mBCohp0FbAkCfn6AaRmMr4`

新建目标 / 打卡 / 补记 / 删记录 / 保存周复盘都即时写回云端；读取失败或写入失败有明确的重试提示，不会白屏、不静默丢数据。

## 四个 Tab
1. **今日**：新建目标入口 + 需处理目标卡（逾期/连续中断红卡、休息日黄卡自动置顶）+ 可直接打卡；支持编辑、二次确认删除。
2. **看板**：进度环 + 完成率/连续天数/预计完成日 + 预案 + 最近 10 条可删记录 + 手写 SVG「近 14 天投入分钟」分目标堆叠柱状图。
3. **周报**：周一至周日投入量/打卡天数/分钟数 + 上周环比 + 历史周切换 + 保持/问题/尝试/下周预案四栏 + 一键生成周报文本。
4. **我的**：在线同步状态与最近同步时间、手动重新同步、清空示例、输入「清空」后清空全部、添加到手机主屏三步说明。

## 核心算法
- 今日建议量 = 剩余量 ÷ 剩余天数（打卡预填可改）。
- 连续天数：每周一始自动允 1 个休息日，本周首次漏打不中断、二次才断。
- 预计完成日：按近 7 天平均速度推算，与截止日比较显示提前/拖后；样本不足显示「暂无推算」（不编数字）。
- 补记最近 6 天带「补」标，按真实日期参与连续天数与推算。
- 目标达成触发彩带动效；图标全内联 SVG，无 emoji。

## 预置示例（已写入线上）
背英语单词、读《人类简史》、Python 入门课 3 个目标 + 多天打卡记录，已覆盖：补记、休息日、连续漏打、障碍预案等状态。可在「我的」里一键清空示例。

## 本地文件
`D:\workbuddy\2026-08-19-20-27-38\learning-goals.html`（已导入并发布）
